name: openxc
description: OpenXC
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^6.0.0
  get_it_hooks: ^2.0.0
  flutter_hooks: ^0.16.0
  flutter_blue: ^0.8.0
  shared_preferences: ^2.0.6
  

flutter:
  uses-material-design: true
  fonts:
      - family: Comfortaa
        fonts:
          - asset: fonts/Comfortaa.ttf